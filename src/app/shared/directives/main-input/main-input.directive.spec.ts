import { MainInputDirective } from './main-input.directive';

describe('MainInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MainInputDirective();
    expect(directive).toBeTruthy();
  });
});
